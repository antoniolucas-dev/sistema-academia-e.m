<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof titulo !== 'undefined' ? titulo + ' | Academia' : 'Academia' %></title>
    <link rel="stylesheet" href="/css/components.css">
    <% if (typeof titulo !== 'undefined') { %>
        <link rel="stylesheet" href="/css/<%= titulo.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g, '') %>.css">
    <% } %>
    <link rel="stylesheet" href="/css/responsive.css">
</head>
<body>